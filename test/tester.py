from file_generator import add_hashes_files
# import master

if __name__ == "__main__":
    add_hashes_files()  # create hashes and results files
    # compare between the results file to the output file (line by line)
